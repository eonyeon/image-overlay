{
  "name": "image-overlay-tool",
  "private": true,
  "version": "1.4.7",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "deploy": "npm run build && npm run tauri build",
    "icons:generate": "node generate-icons.js",
    "icons:fix": "npm run icons:generate && echo 'Icons generated! Now you can run: npm run tauri:build'"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@tauri-apps/api": "^1.5.0",
    "@tauri-apps/cli": "^1.5.0",
    "vite": "^4.4.0"
  }
}